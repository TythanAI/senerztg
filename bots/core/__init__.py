"""Reusable Telegram shop-bot framework (aiogram 3).

A single, config-driven core powers many independent bots. Every bot is one
YAML file in ``config/`` plus its own secrets in the environment; the code
below never changes between bots.
"""

__version__ = "1.0.0"
