# Secrets — never commit filled-in env files
env/*.env
!env/example.env

# Runtime data (SQLite + WAL sidecars)
data/
*.db
*.db-wal
*.db-shm

# Built archives
dist/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
