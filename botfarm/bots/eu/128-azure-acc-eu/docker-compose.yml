# Build from the botfarm root:  docker compose -f bots/eu/128-azure-acc-eu/docker-compose.yml up -d
services:
  botfarm-eu-128-azure-acc-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/128-azure-acc-eu/Dockerfile
    container_name: botfarm-eu-128-azure-acc-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9627:9627"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
