# Build from the botfarm root:  docker compose -f bots/eu/195-edx-eu/docker-compose.yml up -d
services:
  botfarm-eu-195-edx-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/195-edx-eu/Dockerfile
    container_name: botfarm-eu-195-edx-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9694:9694"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
