# Build from the botfarm root:  docker compose -f bots/eu/124-confluence-eu/docker-compose.yml up -d
services:
  botfarm-eu-124-confluence-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/124-confluence-eu/Dockerfile
    container_name: botfarm-eu-124-confluence-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9623:9623"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
