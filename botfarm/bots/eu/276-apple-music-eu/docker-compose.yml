# Build from the botfarm root:  docker compose -f bots/eu/276-apple-music-eu/docker-compose.yml up -d
services:
  botfarm-eu-276-apple-music-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/276-apple-music-eu/Dockerfile
    container_name: botfarm-eu-276-apple-music-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9775:9775"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
