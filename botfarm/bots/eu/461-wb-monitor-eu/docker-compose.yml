# Build from the botfarm root:  docker compose -f bots/eu/461-wb-monitor-eu/docker-compose.yml up -d
services:
  botfarm-eu-461-wb-monitor-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/461-wb-monitor-eu/Dockerfile
    container_name: botfarm-eu-461-wb-monitor-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9960:9960"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
