# Build from the botfarm root:  docker compose -f bots/eu/041-mobility-eu/docker-compose.yml up -d
services:
  botfarm-eu-041-mobility-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/041-mobility-eu/Dockerfile
    container_name: botfarm-eu-041-mobility-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9540:9540"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
