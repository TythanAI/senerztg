# Build from the botfarm root:  docker compose -f bots/eu/041-mobility-eu/docker-compose.yml up -d
services:
  botfarm-eu-mobility-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/041-mobility-eu/Dockerfile
    container_name: botfarm-eu-mobility-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9240:9240"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
