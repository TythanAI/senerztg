# Build from the botfarm root:  docker compose -f bots/eu/370-protonvpn-eu/docker-compose.yml up -d
services:
  botfarm-eu-370-protonvpn-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/370-protonvpn-eu/Dockerfile
    container_name: botfarm-eu-370-protonvpn-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9869:9869"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
