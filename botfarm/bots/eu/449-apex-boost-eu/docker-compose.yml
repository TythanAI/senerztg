# Build from the botfarm root:  docker compose -f bots/eu/449-apex-boost-eu/docker-compose.yml up -d
services:
  botfarm-eu-449-apex-boost-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/449-apex-boost-eu/Dockerfile
    container_name: botfarm-eu-449-apex-boost-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9948:9948"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
