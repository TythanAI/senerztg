# Build from the botfarm root:  docker compose -f bots/eu/482-btc-exchange-eu/docker-compose.yml up -d
services:
  botfarm-eu-482-btc-exchange-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/482-btc-exchange-eu/Dockerfile
    container_name: botfarm-eu-482-btc-exchange-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9981:9981"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
