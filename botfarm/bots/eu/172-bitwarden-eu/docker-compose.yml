# Build from the botfarm root:  docker compose -f bots/eu/172-bitwarden-eu/docker-compose.yml up -d
services:
  botfarm-eu-172-bitwarden-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/172-bitwarden-eu/Dockerfile
    container_name: botfarm-eu-172-bitwarden-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9671:9671"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
