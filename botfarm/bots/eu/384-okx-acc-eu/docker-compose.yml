# Build from the botfarm root:  docker compose -f bots/eu/384-okx-acc-eu/docker-compose.yml up -d
services:
  botfarm-eu-384-okx-acc-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/384-okx-acc-eu/Dockerfile
    container_name: botfarm-eu-384-okx-acc-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9883:9883"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
