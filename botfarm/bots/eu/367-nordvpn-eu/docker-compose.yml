# Build from the botfarm root:  docker compose -f bots/eu/367-nordvpn-eu/docker-compose.yml up -d
services:
  botfarm-eu-367-nordvpn-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/367-nordvpn-eu/Dockerfile
    container_name: botfarm-eu-367-nordvpn-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9866:9866"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
