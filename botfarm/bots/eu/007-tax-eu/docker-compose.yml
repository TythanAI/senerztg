# Build from the botfarm root:  docker compose -f bots/eu/007-tax-eu/docker-compose.yml up -d
services:
  botfarm-eu-007-tax-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/007-tax-eu/Dockerfile
    container_name: botfarm-eu-007-tax-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9506:9506"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
