# Build from the botfarm root:  docker compose -f bots/eu/323-facebook-eu/docker-compose.yml up -d
services:
  botfarm-eu-323-facebook-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/323-facebook-eu/Dockerfile
    container_name: botfarm-eu-323-facebook-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9822:9822"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
