# Build from the botfarm root:  docker compose -f bots/eu/339-protonmail-eu/docker-compose.yml up -d
services:
  botfarm-eu-339-protonmail-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/339-protonmail-eu/Dockerfile
    container_name: botfarm-eu-339-protonmail-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9838:9838"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
