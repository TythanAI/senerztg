# Build from the botfarm root:  docker compose -f bots/eu/395-ebay-acc-eu/docker-compose.yml up -d
services:
  botfarm-eu-395-ebay-acc-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/395-ebay-acc-eu/Dockerfile
    container_name: botfarm-eu-395-ebay-acc-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9894:9894"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
