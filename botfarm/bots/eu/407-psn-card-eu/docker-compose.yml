# Build from the botfarm root:  docker compose -f bots/eu/407-psn-card-eu/docker-compose.yml up -d
services:
  botfarm-eu-407-psn-card-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/407-psn-card-eu/Dockerfile
    container_name: botfarm-eu-407-psn-card-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9906:9906"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
