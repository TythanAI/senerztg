# Build from the botfarm root:  docker compose -f bots/eu/131-vultr-eu/docker-compose.yml up -d
services:
  botfarm-eu-131-vultr-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/131-vultr-eu/Dockerfile
    container_name: botfarm-eu-131-vultr-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9630:9630"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
