# Build from the botfarm root:  docker compose -f bots/eu/307-notion-ai-eu/docker-compose.yml up -d
services:
  botfarm-eu-307-notion-ai-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/307-notion-ai-eu/Dockerfile
    container_name: botfarm-eu-307-notion-ai-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9806:9806"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
