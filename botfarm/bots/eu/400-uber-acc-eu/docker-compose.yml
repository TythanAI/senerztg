# Build from the botfarm root:  docker compose -f bots/eu/400-uber-acc-eu/docker-compose.yml up -d
services:
  botfarm-eu-400-uber-acc-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/400-uber-acc-eu/Dockerfile
    container_name: botfarm-eu-400-uber-acc-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9899:9899"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
