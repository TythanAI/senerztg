# Build from the botfarm root:  docker compose -f bots/eu/083-cooking-eu/docker-compose.yml up -d
services:
  botfarm-eu-cooking-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/083-cooking-eu/Dockerfile
    container_name: botfarm-eu-cooking-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9282:9282"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
