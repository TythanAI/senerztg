# Build from the botfarm root:  docker compose -f bots/eu/029-python-eu/docker-compose.yml up -d
services:
  botfarm-eu-029-python-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/029-python-eu/Dockerfile
    container_name: botfarm-eu-029-python-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9528:9528"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
