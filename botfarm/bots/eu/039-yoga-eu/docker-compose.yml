# Build from the botfarm root:  docker compose -f bots/eu/039-yoga-eu/docker-compose.yml up -d
services:
  botfarm-eu-039-yoga-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/039-yoga-eu/Dockerfile
    container_name: botfarm-eu-039-yoga-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9538:9538"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
