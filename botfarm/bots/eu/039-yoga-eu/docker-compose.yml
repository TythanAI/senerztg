# Build from the botfarm root:  docker compose -f bots/eu/039-yoga-eu/docker-compose.yml up -d
services:
  botfarm-eu-yoga-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/039-yoga-eu/Dockerfile
    container_name: botfarm-eu-yoga-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9238:9238"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
