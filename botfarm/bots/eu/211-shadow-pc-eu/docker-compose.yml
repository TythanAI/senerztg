# Build from the botfarm root:  docker compose -f bots/eu/211-shadow-pc-eu/docker-compose.yml up -d
services:
  botfarm-eu-211-shadow-pc-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/211-shadow-pc-eu/Dockerfile
    container_name: botfarm-eu-211-shadow-pc-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9710:9710"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
