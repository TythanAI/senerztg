# Build from the botfarm root:  docker compose -f bots/eu/110-hotjar-eu/docker-compose.yml up -d
services:
  botfarm-eu-110-hotjar-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/110-hotjar-eu/Dockerfile
    container_name: botfarm-eu-110-hotjar-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9609:9609"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
