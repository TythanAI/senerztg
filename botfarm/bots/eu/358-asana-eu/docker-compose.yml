# Build from the botfarm root:  docker compose -f bots/eu/358-asana-eu/docker-compose.yml up -d
services:
  botfarm-eu-358-asana-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/358-asana-eu/Dockerfile
    container_name: botfarm-eu-358-asana-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9857:9857"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
