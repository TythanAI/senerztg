# Build from the botfarm root:  docker compose -f bots/eu/192-italki-eu/docker-compose.yml up -d
services:
  botfarm-eu-192-italki-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/192-italki-eu/Dockerfile
    container_name: botfarm-eu-192-italki-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9691:9691"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
