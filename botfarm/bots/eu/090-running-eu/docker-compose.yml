# Build from the botfarm root:  docker compose -f bots/eu/090-running-eu/docker-compose.yml up -d
services:
  botfarm-eu-090-running-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/090-running-eu/Dockerfile
    container_name: botfarm-eu-090-running-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9589:9589"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
