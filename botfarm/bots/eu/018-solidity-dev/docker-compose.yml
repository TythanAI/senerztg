# Build from the botfarm root:  docker compose -f bots/eu/018-solidity-dev/docker-compose.yml up -d
services:
  botfarm-eu-solidity-dev:
    build:
      context: ../../..
      dockerfile: bots/eu/018-solidity-dev/Dockerfile
    container_name: botfarm-eu-solidity-dev
    restart: unless-stopped
    env_file: .env
    ports:
      - "9217:9217"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
