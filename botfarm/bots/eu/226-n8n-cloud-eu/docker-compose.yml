# Build from the botfarm root:  docker compose -f bots/eu/226-n8n-cloud-eu/docker-compose.yml up -d
services:
  botfarm-eu-226-n8n-cloud-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/226-n8n-cloud-eu/Dockerfile
    container_name: botfarm-eu-226-n8n-cloud-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9725:9725"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
