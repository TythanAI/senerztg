# Build from the botfarm root:  docker compose -f bots/eu/493-logo-service-eu/docker-compose.yml up -d
services:
  botfarm-eu-493-logo-service-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/493-logo-service-eu/Dockerfile
    container_name: botfarm-eu-493-logo-service-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9992:9992"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
