# Build from the botfarm root:  docker compose -f bots/eu/101-hubspot-eu/docker-compose.yml up -d
services:
  botfarm-eu-101-hubspot-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/101-hubspot-eu/Dockerfile
    container_name: botfarm-eu-101-hubspot-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9600:9600"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
