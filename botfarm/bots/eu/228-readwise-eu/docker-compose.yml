# Build from the botfarm root:  docker compose -f bots/eu/228-readwise-eu/docker-compose.yml up -d
services:
  botfarm-eu-228-readwise-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/228-readwise-eu/Dockerfile
    container_name: botfarm-eu-228-readwise-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9727:9727"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
