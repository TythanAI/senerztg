# Build from the botfarm root:  docker compose -f bots/eu/368-expressvpn-eu/docker-compose.yml up -d
services:
  botfarm-eu-368-expressvpn-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/368-expressvpn-eu/Dockerfile
    container_name: botfarm-eu-368-expressvpn-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9867:9867"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
