# Build from the botfarm root:  docker compose -f bots/eu/440-wow-gold-eu/docker-compose.yml up -d
services:
  botfarm-eu-440-wow-gold-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/440-wow-gold-eu/Dockerfile
    container_name: botfarm-eu-440-wow-gold-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9939:9939"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
