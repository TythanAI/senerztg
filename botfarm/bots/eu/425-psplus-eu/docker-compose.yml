# Build from the botfarm root:  docker compose -f bots/eu/425-psplus-eu/docker-compose.yml up -d
services:
  botfarm-eu-425-psplus-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/425-psplus-eu/Dockerfile
    container_name: botfarm-eu-425-psplus-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9924:9924"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
