# Build from the botfarm root:  docker compose -f bots/eu/204-gamivo-eu/docker-compose.yml up -d
services:
  botfarm-eu-204-gamivo-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/204-gamivo-eu/Dockerfile
    container_name: botfarm-eu-204-gamivo-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9703:9703"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
