# Build from the botfarm root:  docker compose -f bots/eu/451-tarkov-boost-eu/docker-compose.yml up -d
services:
  botfarm-eu-451-tarkov-boost-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/451-tarkov-boost-eu/Dockerfile
    container_name: botfarm-eu-451-tarkov-boost-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9950:9950"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
