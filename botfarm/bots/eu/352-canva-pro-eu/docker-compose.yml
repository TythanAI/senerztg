# Build from the botfarm root:  docker compose -f bots/eu/352-canva-pro-eu/docker-compose.yml up -d
services:
  botfarm-eu-352-canva-pro-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/352-canva-pro-eu/Dockerfile
    container_name: botfarm-eu-352-canva-pro-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9851:9851"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
