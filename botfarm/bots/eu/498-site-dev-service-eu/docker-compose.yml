# Build from the botfarm root:  docker compose -f bots/eu/498-site-dev-service-eu/docker-compose.yml up -d
services:
  botfarm-eu-498-site-dev-service-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/498-site-dev-service-eu/Dockerfile
    container_name: botfarm-eu-498-site-dev-service-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9997:9997"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
