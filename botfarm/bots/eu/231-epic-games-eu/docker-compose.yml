# Build from the botfarm root:  docker compose -f bots/eu/231-epic-games-eu/docker-compose.yml up -d
services:
  botfarm-eu-231-epic-games-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/231-epic-games-eu/Dockerfile
    container_name: botfarm-eu-231-epic-games-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9730:9730"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
