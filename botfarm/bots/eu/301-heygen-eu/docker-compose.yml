# Build from the botfarm root:  docker compose -f bots/eu/301-heygen-eu/docker-compose.yml up -d
services:
  botfarm-eu-301-heygen-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/301-heygen-eu/Dockerfile
    container_name: botfarm-eu-301-heygen-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9800:9800"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
