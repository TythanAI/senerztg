# Build from the botfarm root:  docker compose -f bots/eu/359-miro-eu/docker-compose.yml up -d
services:
  botfarm-eu-359-miro-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/359-miro-eu/Dockerfile
    container_name: botfarm-eu-359-miro-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9858:9858"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
