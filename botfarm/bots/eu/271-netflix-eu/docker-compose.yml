# Build from the botfarm root:  docker compose -f bots/eu/271-netflix-eu/docker-compose.yml up -d
services:
  botfarm-eu-271-netflix-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/271-netflix-eu/Dockerfile
    container_name: botfarm-eu-271-netflix-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9770:9770"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
