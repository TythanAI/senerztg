# Build from the botfarm root:  docker compose -f bots/eu/382-binance-acc-eu/docker-compose.yml up -d
services:
  botfarm-eu-382-binance-acc-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/382-binance-acc-eu/Dockerfile
    container_name: botfarm-eu-382-binance-acc-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9881:9881"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
