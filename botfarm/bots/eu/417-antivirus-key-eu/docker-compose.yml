# Build from the botfarm root:  docker compose -f bots/eu/417-antivirus-key-eu/docker-compose.yml up -d
services:
  botfarm-eu-417-antivirus-key-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/417-antivirus-key-eu/Dockerfile
    container_name: botfarm-eu-417-antivirus-key-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9916:9916"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
