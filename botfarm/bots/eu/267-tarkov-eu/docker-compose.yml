# Build from the botfarm root:  docker compose -f bots/eu/267-tarkov-eu/docker-compose.yml up -d
services:
  botfarm-eu-267-tarkov-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/267-tarkov-eu/Dockerfile
    container_name: botfarm-eu-267-tarkov-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9766:9766"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
