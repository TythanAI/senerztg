# Build from the botfarm root:  docker compose -f bots/eu/040-sleep-eu/docker-compose.yml up -d
services:
  botfarm-eu-sleep-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/040-sleep-eu/Dockerfile
    container_name: botfarm-eu-sleep-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9239:9239"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
