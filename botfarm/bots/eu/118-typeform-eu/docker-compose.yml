# Build from the botfarm root:  docker compose -f bots/eu/118-typeform-eu/docker-compose.yml up -d
services:
  botfarm-eu-118-typeform-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/118-typeform-eu/Dockerfile
    container_name: botfarm-eu-118-typeform-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9617:9617"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
