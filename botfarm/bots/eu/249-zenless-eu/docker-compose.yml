# Build from the botfarm root:  docker compose -f bots/eu/249-zenless-eu/docker-compose.yml up -d
services:
  botfarm-eu-249-zenless-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/249-zenless-eu/Dockerfile
    container_name: botfarm-eu-249-zenless-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9748:9748"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
