# Build from the botfarm root:  docker compose -f bots/eu/057-public-speaking-eu/docker-compose.yml up -d
services:
  botfarm-eu-public-speaking-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/057-public-speaking-eu/Dockerfile
    container_name: botfarm-eu-public-speaking-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9256:9256"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
