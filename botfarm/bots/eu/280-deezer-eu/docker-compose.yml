# Build from the botfarm root:  docker compose -f bots/eu/280-deezer-eu/docker-compose.yml up -d
services:
  botfarm-eu-280-deezer-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/280-deezer-eu/Dockerfile
    container_name: botfarm-eu-280-deezer-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9779:9779"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
