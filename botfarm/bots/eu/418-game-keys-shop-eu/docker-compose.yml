# Build from the botfarm root:  docker compose -f bots/eu/418-game-keys-shop-eu/docker-compose.yml up -d
services:
  botfarm-eu-418-game-keys-shop-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/418-game-keys-shop-eu/Dockerfile
    container_name: botfarm-eu-418-game-keys-shop-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9917:9917"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
