# Build from the botfarm root:  docker compose -f bots/eu/441-steam-wallet-eu/docker-compose.yml up -d
services:
  botfarm-eu-441-steam-wallet-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/441-steam-wallet-eu/Dockerfile
    container_name: botfarm-eu-441-steam-wallet-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9940:9940"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
