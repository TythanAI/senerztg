# Build from the botfarm root:  docker compose -f bots/eu/495-translate-service-eu/docker-compose.yml up -d
services:
  botfarm-eu-495-translate-service-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/495-translate-service-eu/Dockerfile
    container_name: botfarm-eu-495-translate-service-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9994:9994"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
