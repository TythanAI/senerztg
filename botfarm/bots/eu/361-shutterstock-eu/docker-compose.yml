# Build from the botfarm root:  docker compose -f bots/eu/361-shutterstock-eu/docker-compose.yml up -d
services:
  botfarm-eu-361-shutterstock-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/361-shutterstock-eu/Dockerfile
    container_name: botfarm-eu-361-shutterstock-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9860:9860"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
