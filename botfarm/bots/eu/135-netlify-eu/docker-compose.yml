# Build from the botfarm root:  docker compose -f bots/eu/135-netlify-eu/docker-compose.yml up -d
services:
  botfarm-eu-135-netlify-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/135-netlify-eu/Dockerfile
    container_name: botfarm-eu-135-netlify-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9634:9634"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
