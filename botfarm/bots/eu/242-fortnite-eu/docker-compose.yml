# Build from the botfarm root:  docker compose -f bots/eu/242-fortnite-eu/docker-compose.yml up -d
services:
  botfarm-eu-242-fortnite-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/242-fortnite-eu/Dockerfile
    container_name: botfarm-eu-242-fortnite-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9741:9741"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
