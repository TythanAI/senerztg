# Build from the botfarm root:  docker compose -f bots/eu/414-spotify-card-eu/docker-compose.yml up -d
services:
  botfarm-eu-414-spotify-card-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/414-spotify-card-eu/Dockerfile
    container_name: botfarm-eu-414-spotify-card-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9913:9913"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
