# Build from the botfarm root:  docker compose -f bots/eu/020-mining-eu/docker-compose.yml up -d
services:
  botfarm-eu-020-mining-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/020-mining-eu/Dockerfile
    container_name: botfarm-eu-020-mining-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9519:9519"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
