# Build from the botfarm root:  docker compose -f bots/eu/006-budget-app/docker-compose.yml up -d
services:
  botfarm-eu-budget-app:
    build:
      context: ../../..
      dockerfile: bots/eu/006-budget-app/Dockerfile
    container_name: botfarm-eu-budget-app
    restart: unless-stopped
    env_file: .env
    ports:
      - "9205:9205"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
