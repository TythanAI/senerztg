# Build from the botfarm root:  docker compose -f bots/eu/230-steam-eu/docker-compose.yml up -d
services:
  botfarm-eu-230-steam-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/230-steam-eu/Dockerfile
    container_name: botfarm-eu-230-steam-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9729:9729"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
