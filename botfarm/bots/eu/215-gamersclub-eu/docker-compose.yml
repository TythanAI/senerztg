# Build from the botfarm root:  docker compose -f bots/eu/215-gamersclub-eu/docker-compose.yml up -d
services:
  botfarm-eu-215-gamersclub-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/215-gamersclub-eu/Dockerfile
    container_name: botfarm-eu-215-gamersclub-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9714:9714"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
