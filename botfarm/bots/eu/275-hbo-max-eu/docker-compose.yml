# Build from the botfarm root:  docker compose -f bots/eu/275-hbo-max-eu/docker-compose.yml up -d
services:
  botfarm-eu-275-hbo-max-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/275-hbo-max-eu/Dockerfile
    container_name: botfarm-eu-275-hbo-max-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9774:9774"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
