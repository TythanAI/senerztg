# Build from the botfarm root:  docker compose -f bots/eu/004-options-income/docker-compose.yml up -d
services:
  botfarm-eu-004-options-income:
    build:
      context: ../../..
      dockerfile: bots/eu/004-options-income/Dockerfile
    container_name: botfarm-eu-004-options-income
    restart: unless-stopped
    env_file: .env
    ports:
      - "9503:9503"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
