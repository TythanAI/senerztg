# Build from the botfarm root:  docker compose -f bots/eu/259-overwatch-eu/docker-compose.yml up -d
services:
  botfarm-eu-259-overwatch-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/259-overwatch-eu/Dockerfile
    container_name: botfarm-eu-259-overwatch-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9758:9758"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
