# Build from the botfarm root:  docker compose -f bots/eu/311-pika-eu/docker-compose.yml up -d
services:
  botfarm-eu-311-pika-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/311-pika-eu/Dockerfile
    container_name: botfarm-eu-311-pika-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9810:9810"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
