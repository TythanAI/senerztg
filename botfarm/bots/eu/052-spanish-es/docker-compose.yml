# Build from the botfarm root:  docker compose -f bots/eu/052-spanish-es/docker-compose.yml up -d
services:
  botfarm-eu-spanish-es:
    build:
      context: ../../..
      dockerfile: bots/eu/052-spanish-es/Dockerfile
    container_name: botfarm-eu-spanish-es
    restart: unless-stopped
    env_file: .env
    ports:
      - "9251:9251"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
