# Build from the botfarm root:  docker compose -f bots/eu/241-pubg-eu/docker-compose.yml up -d
services:
  botfarm-eu-241-pubg-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/241-pubg-eu/Dockerfile
    container_name: botfarm-eu-241-pubg-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9740:9740"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
