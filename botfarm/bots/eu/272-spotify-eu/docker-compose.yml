# Build from the botfarm root:  docker compose -f bots/eu/272-spotify-eu/docker-compose.yml up -d
services:
  botfarm-eu-272-spotify-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/272-spotify-eu/Dockerfile
    container_name: botfarm-eu-272-spotify-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9771:9771"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
