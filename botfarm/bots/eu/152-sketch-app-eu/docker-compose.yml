# Build from the botfarm root:  docker compose -f bots/eu/152-sketch-app-eu/docker-compose.yml up -d
services:
  botfarm-eu-152-sketch-app-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/152-sketch-app-eu/Dockerfile
    container_name: botfarm-eu-152-sketch-app-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9651:9651"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
