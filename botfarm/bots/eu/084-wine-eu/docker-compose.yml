# Build from the botfarm root:  docker compose -f bots/eu/084-wine-eu/docker-compose.yml up -d
services:
  botfarm-eu-wine-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/084-wine-eu/Dockerfile
    container_name: botfarm-eu-wine-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9283:9283"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
