# Build from the botfarm root:  docker compose -f bots/eu/203-udacity-eu/docker-compose.yml up -d
services:
  botfarm-eu-203-udacity-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/203-udacity-eu/Dockerfile
    container_name: botfarm-eu-203-udacity-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9702:9702"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
