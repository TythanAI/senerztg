# Build from the botfarm root:  docker compose -f bots/eu/138-fly-io-eu/docker-compose.yml up -d
services:
  botfarm-eu-138-fly-io-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/138-fly-io-eu/Dockerfile
    container_name: botfarm-eu-138-fly-io-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9637:9637"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
