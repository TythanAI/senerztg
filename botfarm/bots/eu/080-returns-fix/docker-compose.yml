# Build from the botfarm root:  docker compose -f bots/eu/080-returns-fix/docker-compose.yml up -d
services:
  botfarm-eu-080-returns-fix:
    build:
      context: ../../..
      dockerfile: bots/eu/080-returns-fix/Dockerfile
    container_name: botfarm-eu-080-returns-fix
    restart: unless-stopped
    env_file: .env
    ports:
      - "9579:9579"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
