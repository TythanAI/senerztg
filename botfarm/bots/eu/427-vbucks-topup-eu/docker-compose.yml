# Build from the botfarm root:  docker compose -f bots/eu/427-vbucks-topup-eu/docker-compose.yml up -d
services:
  botfarm-eu-427-vbucks-topup-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/427-vbucks-topup-eu/Dockerfile
    container_name: botfarm-eu-427-vbucks-topup-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9926:9926"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
