# Build from the botfarm root:  docker compose -f bots/eu/144-artlist-eu/docker-compose.yml up -d
services:
  botfarm-eu-144-artlist-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/144-artlist-eu/Dockerfile
    container_name: botfarm-eu-144-artlist-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9643:9643"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
