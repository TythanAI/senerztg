# Build from the botfarm root:  docker compose -f bots/eu/365-duolingo-eu/docker-compose.yml up -d
services:
  botfarm-eu-365-duolingo-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/365-duolingo-eu/Dockerfile
    container_name: botfarm-eu-365-duolingo-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9864:9864"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
