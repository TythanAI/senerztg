# Build from the botfarm root:  docker compose -f bots/eu/347-tutanota-eu/docker-compose.yml up -d
services:
  botfarm-eu-347-tutanota-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/347-tutanota-eu/Dockerfile
    container_name: botfarm-eu-347-tutanota-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9846:9846"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
