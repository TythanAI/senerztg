# Build from the botfarm root:  docker compose -f bots/eu/176-bitdefender-eu/docker-compose.yml up -d
services:
  botfarm-eu-176-bitdefender-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/176-bitdefender-eu/Dockerfile
    container_name: botfarm-eu-176-bitdefender-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9675:9675"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
