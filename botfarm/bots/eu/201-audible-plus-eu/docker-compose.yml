# Build from the botfarm root:  docker compose -f bots/eu/201-audible-plus-eu/docker-compose.yml up -d
services:
  botfarm-eu-201-audible-plus-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/201-audible-plus-eu/Dockerfile
    container_name: botfarm-eu-201-audible-plus-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9700:9700"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
