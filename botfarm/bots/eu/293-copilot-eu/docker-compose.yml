# Build from the botfarm root:  docker compose -f bots/eu/293-copilot-eu/docker-compose.yml up -d
services:
  botfarm-eu-293-copilot-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/293-copilot-eu/Dockerfile
    container_name: botfarm-eu-293-copilot-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9792:9792"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
