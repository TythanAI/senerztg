# Build from the botfarm root:  docker compose -f bots/eu/338-outlook-eu/docker-compose.yml up -d
services:
  botfarm-eu-338-outlook-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/338-outlook-eu/Dockerfile
    container_name: botfarm-eu-338-outlook-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9837:9837"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
