# Build from the botfarm root:  docker compose -f bots/eu/389-gate-io-eu/docker-compose.yml up -d
services:
  botfarm-eu-389-gate-io-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/389-gate-io-eu/Dockerfile
    container_name: botfarm-eu-389-gate-io-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9888:9888"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
