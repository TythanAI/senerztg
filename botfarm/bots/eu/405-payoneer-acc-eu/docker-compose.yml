# Build from the botfarm root:  docker compose -f bots/eu/405-payoneer-acc-eu/docker-compose.yml up -d
services:
  botfarm-eu-405-payoneer-acc-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/405-payoneer-acc-eu/Dockerfile
    container_name: botfarm-eu-405-payoneer-acc-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9904:9904"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
