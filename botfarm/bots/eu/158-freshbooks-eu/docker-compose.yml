# Build from the botfarm root:  docker compose -f bots/eu/158-freshbooks-eu/docker-compose.yml up -d
services:
  botfarm-eu-158-freshbooks-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/158-freshbooks-eu/Dockerfile
    container_name: botfarm-eu-158-freshbooks-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9657:9657"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
