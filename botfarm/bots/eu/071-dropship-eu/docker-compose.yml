# Build from the botfarm root:  docker compose -f bots/eu/071-dropship-eu/docker-compose.yml up -d
services:
  botfarm-eu-dropship-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/071-dropship-eu/Dockerfile
    container_name: botfarm-eu-dropship-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9270:9270"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
