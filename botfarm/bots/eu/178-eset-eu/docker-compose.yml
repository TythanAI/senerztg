# Build from the botfarm root:  docker compose -f bots/eu/178-eset-eu/docker-compose.yml up -d
services:
  botfarm-eu-178-eset-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/178-eset-eu/Dockerfile
    container_name: botfarm-eu-178-eset-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9677:9677"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
