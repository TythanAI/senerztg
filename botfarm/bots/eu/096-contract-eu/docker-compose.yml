# Build from the botfarm root:  docker compose -f bots/eu/096-contract-eu/docker-compose.yml up -d
services:
  botfarm-eu-contract-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/096-contract-eu/Dockerfile
    container_name: botfarm-eu-contract-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9295:9295"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
