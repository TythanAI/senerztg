# Build from the botfarm root:  docker compose -f bots/eu/321-tiktok-eu/docker-compose.yml up -d
services:
  botfarm-eu-321-tiktok-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/321-tiktok-eu/Dockerfile
    container_name: botfarm-eu-321-tiktok-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9820:9820"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
