# Build from the botfarm root:  docker compose -f bots/eu/113-segment-eu/docker-compose.yml up -d
services:
  botfarm-eu-113-segment-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/113-segment-eu/Dockerfile
    container_name: botfarm-eu-113-segment-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9612:9612"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
