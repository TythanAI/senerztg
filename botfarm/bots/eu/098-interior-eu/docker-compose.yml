# Build from the botfarm root:  docker compose -f bots/eu/098-interior-eu/docker-compose.yml up -d
services:
  botfarm-eu-interior-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/098-interior-eu/Dockerfile
    container_name: botfarm-eu-interior-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9297:9297"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
