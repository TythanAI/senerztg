# Build from the botfarm root:  docker compose -f bots/eu/500-domain-service-eu/docker-compose.yml up -d
services:
  botfarm-eu-500-domain-service-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/500-domain-service-eu/Dockerfile
    container_name: botfarm-eu-500-domain-service-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9999:9999"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
