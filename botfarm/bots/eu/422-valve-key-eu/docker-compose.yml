# Build from the botfarm root:  docker compose -f bots/eu/422-valve-key-eu/docker-compose.yml up -d
services:
  botfarm-eu-422-valve-key-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/422-valve-key-eu/Dockerfile
    container_name: botfarm-eu-422-valve-key-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9921:9921"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
