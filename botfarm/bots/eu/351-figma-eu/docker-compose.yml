# Build from the botfarm root:  docker compose -f bots/eu/351-figma-eu/docker-compose.yml up -d
services:
  botfarm-eu-351-figma-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/351-figma-eu/Dockerfile
    container_name: botfarm-eu-351-figma-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9850:9850"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
