# Build from the botfarm root:  docker compose -f bots/eu/167-ft-com-eu/docker-compose.yml up -d
services:
  botfarm-eu-167-ft-com-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/167-ft-com-eu/Dockerfile
    container_name: botfarm-eu-167-ft-com-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9666:9666"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
