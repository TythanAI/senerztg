# Build from the botfarm root:  docker compose -f bots/eu/471-ssl-monitor-eu/docker-compose.yml up -d
services:
  botfarm-eu-471-ssl-monitor-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/471-ssl-monitor-eu/Dockerfile
    container_name: botfarm-eu-471-ssl-monitor-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9970:9970"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
