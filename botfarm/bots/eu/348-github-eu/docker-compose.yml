# Build from the botfarm root:  docker compose -f bots/eu/348-github-eu/docker-compose.yml up -d
services:
  botfarm-eu-348-github-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/348-github-eu/Dockerfile
    container_name: botfarm-eu-348-github-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9847:9847"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
