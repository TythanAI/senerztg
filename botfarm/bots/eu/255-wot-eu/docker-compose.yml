# Build from the botfarm root:  docker compose -f bots/eu/255-wot-eu/docker-compose.yml up -d
services:
  botfarm-eu-255-wot-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/255-wot-eu/Dockerfile
    container_name: botfarm-eu-255-wot-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9754:9754"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
