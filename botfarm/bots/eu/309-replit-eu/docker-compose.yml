# Build from the botfarm root:  docker compose -f bots/eu/309-replit-eu/docker-compose.yml up -d
services:
  botfarm-eu-309-replit-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/309-replit-eu/Dockerfile
    container_name: botfarm-eu-309-replit-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9808:9808"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
