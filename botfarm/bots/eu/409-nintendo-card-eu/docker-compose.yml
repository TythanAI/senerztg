# Build from the botfarm root:  docker compose -f bots/eu/409-nintendo-card-eu/docker-compose.yml up -d
services:
  botfarm-eu-409-nintendo-card-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/409-nintendo-card-eu/Dockerfile
    container_name: botfarm-eu-409-nintendo-card-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9908:9908"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
