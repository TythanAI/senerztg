# Build from the botfarm root:  docker compose -f bots/eu/160-tradingview-eu/docker-compose.yml up -d
services:
  botfarm-eu-160-tradingview-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/160-tradingview-eu/Dockerfile
    container_name: botfarm-eu-160-tradingview-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9659:9659"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
