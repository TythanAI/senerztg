# Build from the botfarm root:  docker compose -f bots/eu/023-api-monitor/docker-compose.yml up -d
services:
  botfarm-eu-023-api-monitor:
    build:
      context: ../../..
      dockerfile: bots/eu/023-api-monitor/Dockerfile
    container_name: botfarm-eu-023-api-monitor
    restart: unless-stopped
    env_file: .env
    ports:
      - "9522:9522"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
