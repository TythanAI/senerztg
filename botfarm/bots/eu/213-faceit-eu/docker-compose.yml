# Build from the botfarm root:  docker compose -f bots/eu/213-faceit-eu/docker-compose.yml up -d
services:
  botfarm-eu-213-faceit-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/213-faceit-eu/Dockerfile
    container_name: botfarm-eu-213-faceit-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9712:9712"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
