# Build from the botfarm root:  docker compose -f bots/eu/388-htx-acc-eu/docker-compose.yml up -d
services:
  botfarm-eu-388-htx-acc-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/388-htx-acc-eu/Dockerfile
    container_name: botfarm-eu-388-htx-acc-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9887:9887"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
