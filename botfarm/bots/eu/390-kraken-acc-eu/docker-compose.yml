# Build from the botfarm root:  docker compose -f bots/eu/390-kraken-acc-eu/docker-compose.yml up -d
services:
  botfarm-eu-390-kraken-acc-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/390-kraken-acc-eu/Dockerfile
    container_name: botfarm-eu-390-kraken-acc-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9889:9889"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
