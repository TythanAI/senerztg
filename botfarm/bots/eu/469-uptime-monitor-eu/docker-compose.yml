# Build from the botfarm root:  docker compose -f bots/eu/469-uptime-monitor-eu/docker-compose.yml up -d
services:
  botfarm-eu-469-uptime-monitor-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/469-uptime-monitor-eu/Dockerfile
    container_name: botfarm-eu-469-uptime-monitor-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9968:9968"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
