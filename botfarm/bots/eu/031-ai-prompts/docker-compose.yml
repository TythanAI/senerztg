# Build from the botfarm root:  docker compose -f bots/eu/031-ai-prompts/docker-compose.yml up -d
services:
  botfarm-eu-031-ai-prompts:
    build:
      context: ../../..
      dockerfile: bots/eu/031-ai-prompts/Dockerfile
    container_name: botfarm-eu-031-ai-prompts
    restart: unless-stopped
    env_file: .env
    ports:
      - "9530:9530"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
