# Build from the botfarm root:  docker compose -f bots/eu/392-bingx-acc-eu/docker-compose.yml up -d
services:
  botfarm-eu-392-bingx-acc-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/392-bingx-acc-eu/Dockerfile
    container_name: botfarm-eu-392-bingx-acc-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9891:9891"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
