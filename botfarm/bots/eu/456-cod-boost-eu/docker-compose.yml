# Build from the botfarm root:  docker compose -f bots/eu/456-cod-boost-eu/docker-compose.yml up -d
services:
  botfarm-eu-456-cod-boost-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/456-cod-boost-eu/Dockerfile
    container_name: botfarm-eu-456-cod-boost-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9955:9955"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
