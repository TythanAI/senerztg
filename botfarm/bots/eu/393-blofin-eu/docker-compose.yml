# Build from the botfarm root:  docker compose -f bots/eu/393-blofin-eu/docker-compose.yml up -d
services:
  botfarm-eu-393-blofin-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/393-blofin-eu/Dockerfile
    container_name: botfarm-eu-393-blofin-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9892:9892"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
