# Build from the botfarm root:  docker compose -f bots/eu/444-discord-boost-eu/docker-compose.yml up -d
services:
  botfarm-eu-444-discord-boost-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/444-discord-boost-eu/Dockerfile
    container_name: botfarm-eu-444-discord-boost-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9943:9943"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
