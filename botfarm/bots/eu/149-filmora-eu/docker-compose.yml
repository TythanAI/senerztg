# Build from the botfarm root:  docker compose -f bots/eu/149-filmora-eu/docker-compose.yml up -d
services:
  botfarm-eu-149-filmora-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/149-filmora-eu/Dockerfile
    container_name: botfarm-eu-149-filmora-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9648:9648"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
