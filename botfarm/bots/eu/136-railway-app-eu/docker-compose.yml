# Build from the botfarm root:  docker compose -f bots/eu/136-railway-app-eu/docker-compose.yml up -d
services:
  botfarm-eu-136-railway-app-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/136-railway-app-eu/Dockerfile
    container_name: botfarm-eu-136-railway-app-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9635:9635"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
