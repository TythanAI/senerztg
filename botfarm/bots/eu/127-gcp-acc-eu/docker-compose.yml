# Build from the botfarm root:  docker compose -f bots/eu/127-gcp-acc-eu/docker-compose.yml up -d
services:
  botfarm-eu-127-gcp-acc-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/127-gcp-acc-eu/Dockerfile
    container_name: botfarm-eu-127-gcp-acc-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9626:9626"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
