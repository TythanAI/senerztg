# Build from the botfarm root:  docker compose -f bots/eu/337-google-workspace-eu/docker-compose.yml up -d
services:
  botfarm-eu-337-google-workspace-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/337-google-workspace-eu/Dockerfile
    container_name: botfarm-eu-337-google-workspace-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9836:9836"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
