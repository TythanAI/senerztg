# Build from the botfarm root:  docker compose -f bots/eu/105-activecampaign-eu/docker-compose.yml up -d
services:
  botfarm-eu-105-activecampaign-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/105-activecampaign-eu/Dockerfile
    container_name: botfarm-eu-105-activecampaign-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9604:9604"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
