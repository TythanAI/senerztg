# Build from the botfarm root:  docker compose -f bots/eu/145-epidemic-sound-eu/docker-compose.yml up -d
services:
  botfarm-eu-145-epidemic-sound-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/145-epidemic-sound-eu/Dockerfile
    container_name: botfarm-eu-145-epidemic-sound-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9644:9644"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
