# Build from the botfarm root:  docker compose -f bots/eu/332-viber-eu/docker-compose.yml up -d
services:
  botfarm-eu-332-viber-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/332-viber-eu/Dockerfile
    container_name: botfarm-eu-332-viber-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9831:9831"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
