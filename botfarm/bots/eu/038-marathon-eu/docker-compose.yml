# Build from the botfarm root:  docker compose -f bots/eu/038-marathon-eu/docker-compose.yml up -d
services:
  botfarm-eu-marathon-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/038-marathon-eu/Dockerfile
    container_name: botfarm-eu-marathon-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9237:9237"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
