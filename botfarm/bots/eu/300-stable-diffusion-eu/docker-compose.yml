# Build from the botfarm root:  docker compose -f bots/eu/300-stable-diffusion-eu/docker-compose.yml up -d
services:
  botfarm-eu-300-stable-diffusion-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/300-stable-diffusion-eu/Dockerfile
    container_name: botfarm-eu-300-stable-diffusion-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9799:9799"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
