# Build from the botfarm root:  docker compose -f bots/eu/289-claude-pro-eu/docker-compose.yml up -d
services:
  botfarm-eu-289-claude-pro-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/289-claude-pro-eu/Dockerfile
    container_name: botfarm-eu-289-claude-pro-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9788:9788"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
