# Build from the botfarm root:  docker compose -f bots/eu/396-aliexpress-acc-eu/docker-compose.yml up -d
services:
  botfarm-eu-396-aliexpress-acc-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/396-aliexpress-acc-eu/Dockerfile
    container_name: botfarm-eu-396-aliexpress-acc-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9895:9895"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
