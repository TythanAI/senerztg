# Build from the botfarm root:  docker compose -f bots/eu/032-no-code-eu/docker-compose.yml up -d
services:
  botfarm-eu-no-code-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/032-no-code-eu/Dockerfile
    container_name: botfarm-eu-no-code-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9231:9231"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
