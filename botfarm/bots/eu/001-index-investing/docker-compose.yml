# Build from the botfarm root:  docker compose -f bots/eu/001-index-investing/docker-compose.yml up -d
services:
  botfarm-eu-index-investing:
    build:
      context: ../../..
      dockerfile: bots/eu/001-index-investing/Dockerfile
    container_name: botfarm-eu-index-investing
    restart: unless-stopped
    env_file: .env
    ports:
      - "9200:9200"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
