# Build from the botfarm root:  docker compose -f bots/eu/099-inmobiliaria-es/docker-compose.yml up -d
services:
  botfarm-eu-inmobiliaria-es:
    build:
      context: ../../..
      dockerfile: bots/eu/099-inmobiliaria-es/Dockerfile
    container_name: botfarm-eu-inmobiliaria-es
    restart: unless-stopped
    env_file: .env
    ports:
      - "9298:9298"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
