# Build from the botfarm root:  docker compose -f bots/eu/104-klaviyo-eu/docker-compose.yml up -d
services:
  botfarm-eu-104-klaviyo-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/104-klaviyo-eu/Dockerfile
    container_name: botfarm-eu-104-klaviyo-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9603:9603"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
