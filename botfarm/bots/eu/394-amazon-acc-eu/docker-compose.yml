# Build from the botfarm root:  docker compose -f bots/eu/394-amazon-acc-eu/docker-compose.yml up -d
services:
  botfarm-eu-394-amazon-acc-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/394-amazon-acc-eu/Dockerfile
    container_name: botfarm-eu-394-amazon-acc-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9893:9893"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
