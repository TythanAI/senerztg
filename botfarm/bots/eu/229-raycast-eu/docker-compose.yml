# Build from the botfarm root:  docker compose -f bots/eu/229-raycast-eu/docker-compose.yml up -d
services:
  botfarm-eu-229-raycast-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/229-raycast-eu/Dockerfile
    container_name: botfarm-eu-229-raycast-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9728:9728"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
