# Build from the botfarm root:  docker compose -f bots/eu/150-davinci-eu/docker-compose.yml up -d
services:
  botfarm-eu-150-davinci-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/150-davinci-eu/Dockerfile
    container_name: botfarm-eu-150-davinci-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9649:9649"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
