# Build from the botfarm root:  docker compose -f bots/eu/117-calendly-eu/docker-compose.yml up -d
services:
  botfarm-eu-117-calendly-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/117-calendly-eu/Dockerfile
    container_name: botfarm-eu-117-calendly-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9616:9616"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
