# Build from the botfarm root:  docker compose -f bots/eu/196-futurelearn-eu/docker-compose.yml up -d
services:
  botfarm-eu-196-futurelearn-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/196-futurelearn-eu/Dockerfile
    container_name: botfarm-eu-196-futurelearn-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9695:9695"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
