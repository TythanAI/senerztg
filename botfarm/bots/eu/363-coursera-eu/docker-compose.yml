# Build from the botfarm root:  docker compose -f bots/eu/363-coursera-eu/docker-compose.yml up -d
services:
  botfarm-eu-363-coursera-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/363-coursera-eu/Dockerfile
    container_name: botfarm-eu-363-coursera-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9862:9862"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
