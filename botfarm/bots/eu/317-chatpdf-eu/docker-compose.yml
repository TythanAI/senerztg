# Build from the botfarm root:  docker compose -f bots/eu/317-chatpdf-eu/docker-compose.yml up -d
services:
  botfarm-eu-317-chatpdf-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/317-chatpdf-eu/Dockerfile
    container_name: botfarm-eu-317-chatpdf-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9816:9816"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
