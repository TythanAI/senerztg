# Build from the botfarm root:  docker compose -f bots/eu/216-evernote-eu/docker-compose.yml up -d
services:
  botfarm-eu-216-evernote-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/216-evernote-eu/Dockerfile
    container_name: botfarm-eu-216-evernote-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9715:9715"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
