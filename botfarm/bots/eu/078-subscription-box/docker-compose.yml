# Build from the botfarm root:  docker compose -f bots/eu/078-subscription-box/docker-compose.yml up -d
services:
  botfarm-eu-subscription-box:
    build:
      context: ../../..
      dockerfile: bots/eu/078-subscription-box/Dockerfile
    container_name: botfarm-eu-subscription-box
    restart: unless-stopped
    env_file: .env
    ports:
      - "9277:9277"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
