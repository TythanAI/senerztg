# Build from the botfarm root:  docker compose -f bots/eu/308-cursor-ai-eu/docker-compose.yml up -d
services:
  botfarm-eu-308-cursor-ai-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/308-cursor-ai-eu/Dockerfile
    container_name: botfarm-eu-308-cursor-ai-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9807:9807"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
