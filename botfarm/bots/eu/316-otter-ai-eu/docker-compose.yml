# Build from the botfarm root:  docker compose -f bots/eu/316-otter-ai-eu/docker-compose.yml up -d
services:
  botfarm-eu-316-otter-ai-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/316-otter-ai-eu/Dockerfile
    container_name: botfarm-eu-316-otter-ai-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9815:9815"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
