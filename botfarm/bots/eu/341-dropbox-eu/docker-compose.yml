# Build from the botfarm root:  docker compose -f bots/eu/341-dropbox-eu/docker-compose.yml up -d
services:
  botfarm-eu-341-dropbox-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/341-dropbox-eu/Dockerfile
    container_name: botfarm-eu-341-dropbox-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9840:9840"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
