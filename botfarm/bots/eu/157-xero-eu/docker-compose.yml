# Build from the botfarm root:  docker compose -f bots/eu/157-xero-eu/docker-compose.yml up -d
services:
  botfarm-eu-157-xero-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/157-xero-eu/Dockerfile
    container_name: botfarm-eu-157-xero-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9656:9656"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
