# Build from the botfarm root:  docker compose -f bots/eu/054-study-abroad/docker-compose.yml up -d
services:
  botfarm-eu-054-study-abroad:
    build:
      context: ../../..
      dockerfile: bots/eu/054-study-abroad/Dockerfile
    container_name: botfarm-eu-054-study-abroad
    restart: unless-stopped
    env_file: .env
    ports:
      - "9553:9553"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
