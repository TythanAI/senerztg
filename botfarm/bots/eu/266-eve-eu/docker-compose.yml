# Build from the botfarm root:  docker compose -f bots/eu/266-eve-eu/docker-compose.yml up -d
services:
  botfarm-eu-266-eve-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/266-eve-eu/Dockerfile
    container_name: botfarm-eu-266-eve-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9765:9765"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
