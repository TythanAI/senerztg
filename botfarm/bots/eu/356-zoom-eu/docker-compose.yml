# Build from the botfarm root:  docker compose -f bots/eu/356-zoom-eu/docker-compose.yml up -d
services:
  botfarm-eu-356-zoom-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/356-zoom-eu/Dockerfile
    container_name: botfarm-eu-356-zoom-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9855:9855"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
