# Build from the botfarm root:  docker compose -f bots/eu/164-bloomberg-lite-eu/docker-compose.yml up -d
services:
  botfarm-eu-164-bloomberg-lite-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/164-bloomberg-lite-eu/Dockerfile
    container_name: botfarm-eu-164-bloomberg-lite-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9663:9663"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
