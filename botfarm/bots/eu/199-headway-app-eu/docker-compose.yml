# Build from the botfarm root:  docker compose -f bots/eu/199-headway-app-eu/docker-compose.yml up -d
services:
  botfarm-eu-199-headway-app-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/199-headway-app-eu/Dockerfile
    container_name: botfarm-eu-199-headway-app-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9698:9698"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
