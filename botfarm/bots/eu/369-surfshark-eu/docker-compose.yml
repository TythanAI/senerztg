# Build from the botfarm root:  docker compose -f bots/eu/369-surfshark-eu/docker-compose.yml up -d
services:
  botfarm-eu-369-surfshark-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/369-surfshark-eu/Dockerfile
    container_name: botfarm-eu-369-surfshark-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9868:9868"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
