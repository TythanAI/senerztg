# Build from the botfarm root:  docker compose -f bots/eu/053-pmp-prep/docker-compose.yml up -d
services:
  botfarm-eu-053-pmp-prep:
    build:
      context: ../../..
      dockerfile: bots/eu/053-pmp-prep/Dockerfile
    container_name: botfarm-eu-053-pmp-prep
    restart: unless-stopped
    env_file: .env
    ports:
      - "9552:9552"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
