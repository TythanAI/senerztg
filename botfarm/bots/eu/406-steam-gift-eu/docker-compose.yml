# Build from the botfarm root:  docker compose -f bots/eu/406-steam-gift-eu/docker-compose.yml up -d
services:
  botfarm-eu-406-steam-gift-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/406-steam-gift-eu/Dockerfile
    container_name: botfarm-eu-406-steam-gift-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9905:9905"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
