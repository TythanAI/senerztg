# Build from the botfarm root:  docker compose -f bots/eu/265-albion-eu/docker-compose.yml up -d
services:
  botfarm-eu-265-albion-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/265-albion-eu/Dockerfile
    container_name: botfarm-eu-265-albion-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9764:9764"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
