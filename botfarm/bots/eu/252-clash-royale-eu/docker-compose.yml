# Build from the botfarm root:  docker compose -f bots/eu/252-clash-royale-eu/docker-compose.yml up -d
services:
  botfarm-eu-252-clash-royale-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/252-clash-royale-eu/Dockerfile
    container_name: botfarm-eu-252-clash-royale-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9751:9751"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
