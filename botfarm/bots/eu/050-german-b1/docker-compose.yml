# Build from the botfarm root:  docker compose -f bots/eu/050-german-b1/docker-compose.yml up -d
services:
  botfarm-eu-050-german-b1:
    build:
      context: ../../..
      dockerfile: bots/eu/050-german-b1/Dockerfile
    container_name: botfarm-eu-050-german-b1
    restart: unless-stopped
    env_file: .env
    ports:
      - "9549:9549"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
