# Build from the botfarm root:  docker compose -f bots/eu/142-getty-eu/docker-compose.yml up -d
services:
  botfarm-eu-142-getty-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/142-getty-eu/Dockerfile
    container_name: botfarm-eu-142-getty-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9641:9641"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
