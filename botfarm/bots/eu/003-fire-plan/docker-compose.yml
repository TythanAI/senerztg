# Build from the botfarm root:  docker compose -f bots/eu/003-fire-plan/docker-compose.yml up -d
services:
  botfarm-eu-003-fire-plan:
    build:
      context: ../../..
      dockerfile: bots/eu/003-fire-plan/Dockerfile
    container_name: botfarm-eu-003-fire-plan
    restart: unless-stopped
    env_file: .env
    ports:
      - "9502:9502"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
