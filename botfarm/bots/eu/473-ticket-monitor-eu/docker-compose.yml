# Build from the botfarm root:  docker compose -f bots/eu/473-ticket-monitor-eu/docker-compose.yml up -d
services:
  botfarm-eu-473-ticket-monitor-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/473-ticket-monitor-eu/Dockerfile
    container_name: botfarm-eu-473-ticket-monitor-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9972:9972"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
