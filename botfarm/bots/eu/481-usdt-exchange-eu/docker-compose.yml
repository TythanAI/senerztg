# Build from the botfarm root:  docker compose -f bots/eu/481-usdt-exchange-eu/docker-compose.yml up -d
services:
  botfarm-eu-481-usdt-exchange-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/481-usdt-exchange-eu/Dockerfile
    container_name: botfarm-eu-481-usdt-exchange-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9980:9980"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
