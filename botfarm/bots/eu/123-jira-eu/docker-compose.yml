# Build from the botfarm root:  docker compose -f bots/eu/123-jira-eu/docker-compose.yml up -d
services:
  botfarm-eu-123-jira-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/123-jira-eu/Dockerfile
    container_name: botfarm-eu-123-jira-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9622:9622"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
