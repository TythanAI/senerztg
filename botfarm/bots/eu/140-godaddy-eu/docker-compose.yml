# Build from the botfarm root:  docker compose -f bots/eu/140-godaddy-eu/docker-compose.yml up -d
services:
  botfarm-eu-140-godaddy-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/140-godaddy-eu/Dockerfile
    container_name: botfarm-eu-140-godaddy-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9639:9639"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
