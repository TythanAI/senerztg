# Build from the botfarm root:  docker compose -f bots/eu/315-deepl-eu/docker-compose.yml up -d
services:
  botfarm-eu-315-deepl-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/315-deepl-eu/Dockerfile
    container_name: botfarm-eu-315-deepl-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9814:9814"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
