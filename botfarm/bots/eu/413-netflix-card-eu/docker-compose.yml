# Build from the botfarm root:  docker compose -f bots/eu/413-netflix-card-eu/docker-compose.yml up -d
services:
  botfarm-eu-413-netflix-card-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/413-netflix-card-eu/Dockerfile
    container_name: botfarm-eu-413-netflix-card-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9912:9912"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
