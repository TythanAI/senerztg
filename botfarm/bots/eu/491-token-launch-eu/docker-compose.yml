# Build from the botfarm root:  docker compose -f bots/eu/491-token-launch-eu/docker-compose.yml up -d
services:
  botfarm-eu-491-token-launch-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/491-token-launch-eu/Dockerfile
    container_name: botfarm-eu-491-token-launch-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9990:9990"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
