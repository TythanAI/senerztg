# Build from the botfarm root:  docker compose -f bots/eu/448-lol-boost-eu/docker-compose.yml up -d
services:
  botfarm-eu-448-lol-boost-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/448-lol-boost-eu/Dockerfile
    container_name: botfarm-eu-448-lol-boost-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9947:9947"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
