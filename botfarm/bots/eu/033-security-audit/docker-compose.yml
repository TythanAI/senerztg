# Build from the botfarm root:  docker compose -f bots/eu/033-security-audit/docker-compose.yml up -d
services:
  botfarm-eu-033-security-audit:
    build:
      context: ../../..
      dockerfile: bots/eu/033-security-audit/Dockerfile
    container_name: botfarm-eu-033-security-audit
    restart: unless-stopped
    env_file: .env
    ports:
      - "9532:9532"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
