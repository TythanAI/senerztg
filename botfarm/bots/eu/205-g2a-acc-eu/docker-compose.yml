# Build from the botfarm root:  docker compose -f bots/eu/205-g2a-acc-eu/docker-compose.yml up -d
services:
  botfarm-eu-205-g2a-acc-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/205-g2a-acc-eu/Dockerfile
    container_name: botfarm-eu-205-g2a-acc-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9704:9704"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
