# Build from the botfarm root:  docker compose -f bots/eu/303-descript-eu/docker-compose.yml up -d
services:
  botfarm-eu-303-descript-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/303-descript-eu/Dockerfile
    container_name: botfarm-eu-303-descript-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9802:9802"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
