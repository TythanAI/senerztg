# Build from the botfarm root:  docker compose -f bots/eu/439-honkai-crystals-eu/docker-compose.yml up -d
services:
  botfarm-eu-439-honkai-crystals-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/439-honkai-crystals-eu/Dockerfile
    container_name: botfarm-eu-439-honkai-crystals-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9938:9938"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
