# Build from the botfarm root:  docker compose -f bots/eu/132-linode-eu/docker-compose.yml up -d
services:
  botfarm-eu-132-linode-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/132-linode-eu/Dockerfile
    container_name: botfarm-eu-132-linode-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9631:9631"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
