# Build from the botfarm root:  docker compose -f bots/eu/082-video-eu/docker-compose.yml up -d
services:
  botfarm-eu-082-video-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/082-video-eu/Dockerfile
    container_name: botfarm-eu-082-video-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9581:9581"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
