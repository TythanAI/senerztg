# Build from the botfarm root:  docker compose -f bots/eu/313-krea-eu/docker-compose.yml up -d
services:
  botfarm-eu-313-krea-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/313-krea-eu/Dockerfile
    container_name: botfarm-eu-313-krea-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9812:9812"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
