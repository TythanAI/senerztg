# Build from the botfarm root:  docker compose -f bots/eu/424-gamepass-eu/docker-compose.yml up -d
services:
  botfarm-eu-424-gamepass-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/424-gamepass-eu/Dockerfile
    container_name: botfarm-eu-424-gamepass-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9923:9923"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
