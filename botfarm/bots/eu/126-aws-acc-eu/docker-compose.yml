# Build from the botfarm root:  docker compose -f bots/eu/126-aws-acc-eu/docker-compose.yml up -d
services:
  botfarm-eu-126-aws-acc-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/126-aws-acc-eu/Dockerfile
    container_name: botfarm-eu-126-aws-acc-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9625:9625"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
