# Build from the botfarm root:  docker compose -f bots/eu/403-wise-acc-eu/docker-compose.yml up -d
services:
  botfarm-eu-403-wise-acc-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/403-wise-acc-eu/Dockerfile
    container_name: botfarm-eu-403-wise-acc-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9902:9902"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
