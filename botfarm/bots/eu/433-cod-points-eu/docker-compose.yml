# Build from the botfarm root:  docker compose -f bots/eu/433-cod-points-eu/docker-compose.yml up -d
services:
  botfarm-eu-433-cod-points-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/433-cod-points-eu/Dockerfile
    container_name: botfarm-eu-433-cod-points-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9932:9932"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
