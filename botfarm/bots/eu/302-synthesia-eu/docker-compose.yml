# Build from the botfarm root:  docker compose -f bots/eu/302-synthesia-eu/docker-compose.yml up -d
services:
  botfarm-eu-302-synthesia-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/302-synthesia-eu/Dockerfile
    container_name: botfarm-eu-302-synthesia-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9801:9801"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
