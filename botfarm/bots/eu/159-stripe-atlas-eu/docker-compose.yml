# Build from the botfarm root:  docker compose -f bots/eu/159-stripe-atlas-eu/docker-compose.yml up -d
services:
  botfarm-eu-159-stripe-atlas-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/159-stripe-atlas-eu/Dockerfile
    container_name: botfarm-eu-159-stripe-atlas-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9658:9658"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
