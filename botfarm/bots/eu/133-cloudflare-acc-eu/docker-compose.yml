# Build from the botfarm root:  docker compose -f bots/eu/133-cloudflare-acc-eu/docker-compose.yml up -d
services:
  botfarm-eu-133-cloudflare-acc-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/133-cloudflare-acc-eu/Dockerfile
    container_name: botfarm-eu-133-cloudflare-acc-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9632:9632"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
