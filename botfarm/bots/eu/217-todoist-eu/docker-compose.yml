# Build from the botfarm root:  docker compose -f bots/eu/217-todoist-eu/docker-compose.yml up -d
services:
  botfarm-eu-217-todoist-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/217-todoist-eu/Dockerfile
    container_name: botfarm-eu-217-todoist-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9716:9716"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
