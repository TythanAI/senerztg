# Build from the botfarm root:  docker compose -f bots/eu/354-notion-eu/docker-compose.yml up -d
services:
  botfarm-eu-354-notion-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/354-notion-eu/Dockerfile
    container_name: botfarm-eu-354-notion-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9853:9853"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
