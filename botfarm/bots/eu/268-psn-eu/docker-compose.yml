# Build from the botfarm root:  docker compose -f bots/eu/268-psn-eu/docker-compose.yml up -d
services:
  botfarm-eu-268-psn-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/268-psn-eu/Dockerfile
    container_name: botfarm-eu-268-psn-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9767:9767"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
