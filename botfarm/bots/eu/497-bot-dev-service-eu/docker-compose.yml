# Build from the botfarm root:  docker compose -f bots/eu/497-bot-dev-service-eu/docker-compose.yml up -d
services:
  botfarm-eu-497-bot-dev-service-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/497-bot-dev-service-eu/Dockerfile
    container_name: botfarm-eu-497-bot-dev-service-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9996:9996"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
