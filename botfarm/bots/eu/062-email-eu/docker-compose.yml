# Build from the botfarm root:  docker compose -f bots/eu/062-email-eu/docker-compose.yml up -d
services:
  botfarm-eu-email-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/062-email-eu/Dockerfile
    container_name: botfarm-eu-email-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9261:9261"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
