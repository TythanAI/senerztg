# Build from the botfarm root:  docker compose -f bots/eu/081-photo-eu/docker-compose.yml up -d
services:
  botfarm-eu-photo-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/081-photo-eu/Dockerfile
    container_name: botfarm-eu-photo-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9280:9280"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
