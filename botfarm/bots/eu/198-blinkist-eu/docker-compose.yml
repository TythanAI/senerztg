# Build from the botfarm root:  docker compose -f bots/eu/198-blinkist-eu/docker-compose.yml up -d
services:
  botfarm-eu-198-blinkist-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/198-blinkist-eu/Dockerfile
    container_name: botfarm-eu-198-blinkist-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9697:9697"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
