# Build from the botfarm root:  docker compose -f bots/eu/019-airdrop-eu/docker-compose.yml up -d
services:
  botfarm-eu-airdrop-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/019-airdrop-eu/Dockerfile
    container_name: botfarm-eu-airdrop-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9218:9218"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
