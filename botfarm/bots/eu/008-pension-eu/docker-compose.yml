# Build from the botfarm root:  docker compose -f bots/eu/008-pension-eu/docker-compose.yml up -d
services:
  botfarm-eu-pension-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/008-pension-eu/Dockerfile
    container_name: botfarm-eu-pension-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9207:9207"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
