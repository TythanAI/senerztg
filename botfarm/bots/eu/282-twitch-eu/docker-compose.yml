# Build from the botfarm root:  docker compose -f bots/eu/282-twitch-eu/docker-compose.yml up -d
services:
  botfarm-eu-282-twitch-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/282-twitch-eu/Dockerfile
    container_name: botfarm-eu-282-twitch-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9781:9781"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
