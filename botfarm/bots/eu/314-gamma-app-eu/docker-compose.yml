# Build from the botfarm root:  docker compose -f bots/eu/314-gamma-app-eu/docker-compose.yml up -d
services:
  botfarm-eu-314-gamma-app-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/314-gamma-app-eu/Dockerfile
    container_name: botfarm-eu-314-gamma-app-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9813:9813"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
