# Build from the botfarm root:  docker compose -f bots/eu/379-outline-vpn-eu/docker-compose.yml up -d
services:
  botfarm-eu-379-outline-vpn-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/379-outline-vpn-eu/Dockerfile
    container_name: botfarm-eu-379-outline-vpn-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9878:9878"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
