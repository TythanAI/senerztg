# Build from the botfarm root:  docker compose -f bots/eu/435-lol-rp-eu/docker-compose.yml up -d
services:
  botfarm-eu-435-lol-rp-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/435-lol-rp-eu/Dockerfile
    container_name: botfarm-eu-435-lol-rp-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9934:9934"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
