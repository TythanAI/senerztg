# Build from the botfarm root:  docker compose -f bots/eu/139-namecheap-eu/docker-compose.yml up -d
services:
  botfarm-eu-139-namecheap-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/139-namecheap-eu/Dockerfile
    container_name: botfarm-eu-139-namecheap-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9638:9638"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
