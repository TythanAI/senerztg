# Build from the botfarm root:  docker compose -f bots/eu/345-zoho-mail-eu/docker-compose.yml up -d
services:
  botfarm-eu-345-zoho-mail-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/345-zoho-mail-eu/Dockerfile
    container_name: botfarm-eu-345-zoho-mail-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9844:9844"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
