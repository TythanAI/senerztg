# Build from the botfarm root:  docker compose -f bots/eu/381-sms-numbers-eu/docker-compose.yml up -d
services:
  botfarm-eu-381-sms-numbers-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/381-sms-numbers-eu/Dockerfile
    container_name: botfarm-eu-381-sms-numbers-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9880:9880"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
