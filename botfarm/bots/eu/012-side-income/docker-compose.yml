# Build from the botfarm root:  docker compose -f bots/eu/012-side-income/docker-compose.yml up -d
services:
  botfarm-eu-012-side-income:
    build:
      context: ../../..
      dockerfile: bots/eu/012-side-income/Dockerfile
    container_name: botfarm-eu-012-side-income
    restart: unless-stopped
    env_file: .env
    ports:
      - "9511:9511"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
