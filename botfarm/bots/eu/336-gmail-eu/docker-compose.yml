# Build from the botfarm root:  docker compose -f bots/eu/336-gmail-eu/docker-compose.yml up -d
services:
  botfarm-eu-336-gmail-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/336-gmail-eu/Dockerfile
    container_name: botfarm-eu-336-gmail-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9835:9835"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
