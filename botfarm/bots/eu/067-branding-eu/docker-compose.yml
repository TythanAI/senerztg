# Build from the botfarm root:  docker compose -f bots/eu/067-branding-eu/docker-compose.yml up -d
services:
  botfarm-eu-branding-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/067-branding-eu/Dockerfile
    container_name: botfarm-eu-branding-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9266:9266"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
