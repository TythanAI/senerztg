# Build from the botfarm root:  docker compose -f bots/eu/091-relocate-eu/docker-compose.yml up -d
services:
  botfarm-eu-relocate-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/091-relocate-eu/Dockerfile
    container_name: botfarm-eu-relocate-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9290:9290"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
