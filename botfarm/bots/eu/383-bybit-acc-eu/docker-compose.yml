# Build from the botfarm root:  docker compose -f bots/eu/383-bybit-acc-eu/docker-compose.yml up -d
services:
  botfarm-eu-383-bybit-acc-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/383-bybit-acc-eu/Dockerfile
    container_name: botfarm-eu-383-bybit-acc-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9882:9882"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
