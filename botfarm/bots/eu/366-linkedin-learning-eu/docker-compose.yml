# Build from the botfarm root:  docker compose -f bots/eu/366-linkedin-learning-eu/docker-compose.yml up -d
services:
  botfarm-eu-366-linkedin-learning-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/366-linkedin-learning-eu/Dockerfile
    container_name: botfarm-eu-366-linkedin-learning-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9865:9865"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
