# Build from the botfarm root:  docker compose -f bots/eu/015-onchain-signals/docker-compose.yml up -d
services:
  botfarm-eu-onchain-signals:
    build:
      context: ../../..
      dockerfile: bots/eu/015-onchain-signals/Dockerfile
    container_name: botfarm-eu-onchain-signals
    restart: unless-stopped
    env_file: .env
    ports:
      - "9214:9214"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
