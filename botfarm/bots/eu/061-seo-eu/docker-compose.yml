# Build from the botfarm root:  docker compose -f bots/eu/061-seo-eu/docker-compose.yml up -d
services:
  botfarm-eu-seo-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/061-seo-eu/Dockerfile
    container_name: botfarm-eu-seo-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9260:9260"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
