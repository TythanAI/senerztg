# Build from the botfarm root:  docker compose -f bots/eu/331-whatsapp-eu/docker-compose.yml up -d
services:
  botfarm-eu-331-whatsapp-eu:
    build:
      context: ../../..
      dockerfile: bots/eu/331-whatsapp-eu/Dockerfile
    container_name: botfarm-eu-331-whatsapp-eu
    restart: unless-stopped
    env_file: .env
    ports:
      - "9830:9830"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
