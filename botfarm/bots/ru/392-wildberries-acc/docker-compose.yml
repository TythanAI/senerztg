# Build from the botfarm root:  docker compose -f bots/ru/392-wildberries-acc/docker-compose.yml up -d
services:
  botfarm-ru-392-wildberries-acc:
    build:
      context: ../../..
      dockerfile: bots/ru/392-wildberries-acc/Dockerfile
    container_name: botfarm-ru-392-wildberries-acc
    restart: unless-stopped
    env_file: .env
    ports:
      - "9391:9391"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
