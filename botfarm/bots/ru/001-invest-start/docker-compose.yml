# Build from the botfarm root:  docker compose -f bots/ru/001-invest-start/docker-compose.yml up -d
services:
  botfarm-ru-invest-start:
    build:
      context: ../../..
      dockerfile: bots/ru/001-invest-start/Dockerfile
    container_name: botfarm-ru-invest-start
    restart: unless-stopped
    env_file: .env
    ports:
      - "9000:9000"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
