# Build from the botfarm root:  docker compose -f bots/ru/161-moto-school/docker-compose.yml up -d
services:
  botfarm-ru-moto-school:
    build:
      context: ../../..
      dockerfile: bots/ru/161-moto-school/Dockerfile
    container_name: botfarm-ru-moto-school
    restart: unless-stopped
    env_file: .env
    ports:
      - "9160:9160"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
