# Build from the botfarm root:  docker compose -f bots/ru/440-wow-gold/docker-compose.yml up -d
services:
  botfarm-ru-440-wow-gold:
    build:
      context: ../../..
      dockerfile: bots/ru/440-wow-gold/Dockerfile
    container_name: botfarm-ru-440-wow-gold
    restart: unless-stopped
    env_file: .env
    ports:
      - "9439:9439"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
