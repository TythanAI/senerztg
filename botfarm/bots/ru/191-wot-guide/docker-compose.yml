# Build from the botfarm root:  docker compose -f bots/ru/191-wot-guide/docker-compose.yml up -d
services:
  botfarm-ru-wot-guide:
    build:
      context: ../../..
      dockerfile: bots/ru/191-wot-guide/Dockerfile
    container_name: botfarm-ru-wot-guide
    restart: unless-stopped
    env_file: .env
    ports:
      - "9190:9190"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
