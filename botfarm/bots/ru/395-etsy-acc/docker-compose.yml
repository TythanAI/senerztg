# Build from the botfarm root:  docker compose -f bots/ru/395-etsy-acc/docker-compose.yml up -d
services:
  botfarm-ru-395-etsy-acc:
    build:
      context: ../../..
      dockerfile: bots/ru/395-etsy-acc/Dockerfile
    container_name: botfarm-ru-395-etsy-acc
    restart: unless-stopped
    env_file: .env
    ports:
      - "9394:9394"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
