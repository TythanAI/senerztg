# Build from the botfarm root:  docker compose -f bots/ru/479-auction-monitor/docker-compose.yml up -d
services:
  botfarm-ru-479-auction-monitor:
    build:
      context: ../../..
      dockerfile: bots/ru/479-auction-monitor/Dockerfile
    container_name: botfarm-ru-479-auction-monitor
    restart: unless-stopped
    env_file: .env
    ports:
      - "9478:9478"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
