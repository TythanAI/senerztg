# Build from the botfarm root:  docker compose -f bots/ru/076-style-guide/docker-compose.yml up -d
services:
  botfarm-ru-style-guide:
    build:
      context: ../../..
      dockerfile: bots/ru/076-style-guide/Dockerfile
    container_name: botfarm-ru-style-guide
    restart: unless-stopped
    env_file: .env
    ports:
      - "9075:9075"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
