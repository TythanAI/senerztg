# Build from the botfarm root:  docker compose -f bots/ru/401-paypal-acc/docker-compose.yml up -d
services:
  botfarm-ru-401-paypal-acc:
    build:
      context: ../../..
      dockerfile: bots/ru/401-paypal-acc/Dockerfile
    container_name: botfarm-ru-401-paypal-acc
    restart: unless-stopped
    env_file: .env
    ports:
      - "9400:9400"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
