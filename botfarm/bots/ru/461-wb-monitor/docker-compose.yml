# Build from the botfarm root:  docker compose -f bots/ru/461-wb-monitor/docker-compose.yml up -d
services:
  botfarm-ru-461-wb-monitor:
    build:
      context: ../../..
      dockerfile: bots/ru/461-wb-monitor/Dockerfile
    container_name: botfarm-ru-461-wb-monitor
    restart: unless-stopped
    env_file: .env
    ports:
      - "9460:9460"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
