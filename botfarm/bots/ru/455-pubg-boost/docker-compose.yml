# Build from the botfarm root:  docker compose -f bots/ru/455-pubg-boost/docker-compose.yml up -d
services:
  botfarm-ru-455-pubg-boost:
    build:
      context: ../../..
      dockerfile: bots/ru/455-pubg-boost/Dockerfile
    container_name: botfarm-ru-455-pubg-boost
    restart: unless-stopped
    env_file: .env
    ports:
      - "9454:9454"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
