# Build from the botfarm root:  docker compose -f bots/ru/092-seo-boost/docker-compose.yml up -d
services:
  botfarm-ru-092-seo-boost:
    build:
      context: ../../..
      dockerfile: bots/ru/092-seo-boost/Dockerfile
    container_name: botfarm-ru-092-seo-boost
    restart: unless-stopped
    env_file: .env
    ports:
      - "9091:9091"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
