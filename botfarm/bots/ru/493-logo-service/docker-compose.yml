# Build from the botfarm root:  docker compose -f bots/ru/493-logo-service/docker-compose.yml up -d
services:
  botfarm-ru-493-logo-service:
    build:
      context: ../../..
      dockerfile: bots/ru/493-logo-service/Dockerfile
    container_name: botfarm-ru-493-logo-service
    restart: unless-stopped
    env_file: .env
    ports:
      - "9492:9492"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
