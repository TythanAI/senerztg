# Build from the botfarm root:  docker compose -f bots/ru/463-avito-monitor/docker-compose.yml up -d
services:
  botfarm-ru-463-avito-monitor:
    build:
      context: ../../..
      dockerfile: bots/ru/463-avito-monitor/Dockerfile
    container_name: botfarm-ru-463-avito-monitor
    restart: unless-stopped
    env_file: .env
    ports:
      - "9462:9462"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
