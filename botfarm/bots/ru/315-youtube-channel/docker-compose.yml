# Build from the botfarm root:  docker compose -f bots/ru/315-youtube-channel/docker-compose.yml up -d
services:
  botfarm-ru-315-youtube-channel:
    build:
      context: ../../..
      dockerfile: bots/ru/315-youtube-channel/Dockerfile
    container_name: botfarm-ru-315-youtube-channel
    restart: unless-stopped
    env_file: .env
    ports:
      - "9314:9314"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
