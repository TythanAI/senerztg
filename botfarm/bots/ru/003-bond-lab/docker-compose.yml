# Build from the botfarm root:  docker compose -f bots/ru/003-bond-lab/docker-compose.yml up -d
services:
  botfarm-ru-bond-lab:
    build:
      context: ../../..
      dockerfile: bots/ru/003-bond-lab/Dockerfile
    container_name: botfarm-ru-bond-lab
    restart: unless-stopped
    env_file: .env
    ports:
      - "9002:9002"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
