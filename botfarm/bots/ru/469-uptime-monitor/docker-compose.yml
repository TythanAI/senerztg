# Build from the botfarm root:  docker compose -f bots/ru/469-uptime-monitor/docker-compose.yml up -d
services:
  botfarm-ru-469-uptime-monitor:
    build:
      context: ../../..
      dockerfile: bots/ru/469-uptime-monitor/Dockerfile
    container_name: botfarm-ru-469-uptime-monitor
    restart: unless-stopped
    env_file: .env
    ports:
      - "9468:9468"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
