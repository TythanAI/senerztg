# Build from the botfarm root:  docker compose -f bots/ru/472-sneaker-monitor/docker-compose.yml up -d
services:
  botfarm-ru-472-sneaker-monitor:
    build:
      context: ../../..
      dockerfile: bots/ru/472-sneaker-monitor/Dockerfile
    container_name: botfarm-ru-472-sneaker-monitor
    restart: unless-stopped
    env_file: .env
    ports:
      - "9471:9471"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
