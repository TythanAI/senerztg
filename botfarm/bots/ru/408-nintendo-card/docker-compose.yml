# Build from the botfarm root:  docker compose -f bots/ru/408-nintendo-card/docker-compose.yml up -d
services:
  botfarm-ru-408-nintendo-card:
    build:
      context: ../../..
      dockerfile: bots/ru/408-nintendo-card/Dockerfile
    container_name: botfarm-ru-408-nintendo-card
    restart: unless-stopped
    env_file: .env
    ports:
      - "9407:9407"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
