# Build from the botfarm root:  docker compose -f bots/ru/005-finplan-pro/docker-compose.yml up -d
services:
  botfarm-ru-005-finplan-pro:
    build:
      context: ../../..
      dockerfile: bots/ru/005-finplan-pro/Dockerfile
    container_name: botfarm-ru-005-finplan-pro
    restart: unless-stopped
    env_file: .env
    ports:
      - "9004:9004"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
