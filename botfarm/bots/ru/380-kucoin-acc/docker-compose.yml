# Build from the botfarm root:  docker compose -f bots/ru/380-kucoin-acc/docker-compose.yml up -d
services:
  botfarm-ru-380-kucoin-acc:
    build:
      context: ../../..
      dockerfile: bots/ru/380-kucoin-acc/Dockerfile
    container_name: botfarm-ru-380-kucoin-acc
    restart: unless-stopped
    env_file: .env
    ports:
      - "9379:9379"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
