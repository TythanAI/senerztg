# Build from the botfarm root:  docker compose -f bots/ru/346-canva-pro/docker-compose.yml up -d
services:
  botfarm-ru-346-canva-pro:
    build:
      context: ../../..
      dockerfile: bots/ru/346-canva-pro/Dockerfile
    container_name: botfarm-ru-346-canva-pro
    restart: unless-stopped
    env_file: .env
    ports:
      - "9345:9345"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
