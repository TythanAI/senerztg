# Build from the botfarm root:  docker compose -f bots/ru/396-upwork-acc/docker-compose.yml up -d
services:
  botfarm-ru-396-upwork-acc:
    build:
      context: ../../..
      dockerfile: bots/ru/396-upwork-acc/Dockerfile
    container_name: botfarm-ru-396-upwork-acc
    restart: unless-stopped
    env_file: .env
    ports:
      - "9395:9395"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
