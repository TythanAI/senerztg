# Build from the botfarm root:  docker compose -f bots/ru/371-residential-proxy/docker-compose.yml up -d
services:
  botfarm-ru-371-residential-proxy:
    build:
      context: ../../..
      dockerfile: bots/ru/371-residential-proxy/Dockerfile
    container_name: botfarm-ru-371-residential-proxy
    restart: unless-stopped
    env_file: .env
    ports:
      - "9370:9370"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
