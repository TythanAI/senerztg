# Build from the botfarm root:  docker compose -f bots/ru/165-rent-flow/docker-compose.yml up -d
services:
  botfarm-ru-rent-flow:
    build:
      context: ../../..
      dockerfile: bots/ru/165-rent-flow/Dockerfile
    container_name: botfarm-ru-rent-flow
    restart: unless-stopped
    env_file: .env
    ports:
      - "9164:9164"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
