# Build from the botfarm root:  docker compose -f bots/ru/394-aliexpress-acc/docker-compose.yml up -d
services:
  botfarm-ru-394-aliexpress-acc:
    build:
      context: ../../..
      dockerfile: bots/ru/394-aliexpress-acc/Dockerfile
    container_name: botfarm-ru-394-aliexpress-acc
    restart: unless-stopped
    env_file: .env
    ports:
      - "9393:9393"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
