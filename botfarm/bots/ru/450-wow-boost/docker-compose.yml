# Build from the botfarm root:  docker compose -f bots/ru/450-wow-boost/docker-compose.yml up -d
services:
  botfarm-ru-450-wow-boost:
    build:
      context: ../../..
      dockerfile: bots/ru/450-wow-boost/Dockerfile
    container_name: botfarm-ru-450-wow-boost
    restart: unless-stopped
    env_file: .env
    ports:
      - "9449:9449"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
