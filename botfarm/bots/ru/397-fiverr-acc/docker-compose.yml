# Build from the botfarm root:  docker compose -f bots/ru/397-fiverr-acc/docker-compose.yml up -d
services:
  botfarm-ru-397-fiverr-acc:
    build:
      context: ../../..
      dockerfile: bots/ru/397-fiverr-acc/Dockerfile
    container_name: botfarm-ru-397-fiverr-acc
    restart: unless-stopped
    env_file: .env
    ports:
      - "9396:9396"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
