# Build from the botfarm root:  docker compose -f bots/ru/451-tarkov-boost/docker-compose.yml up -d
services:
  botfarm-ru-451-tarkov-boost:
    build:
      context: ../../..
      dockerfile: bots/ru/451-tarkov-boost/Dockerfile
    container_name: botfarm-ru-451-tarkov-boost
    restart: unless-stopped
    env_file: .env
    ports:
      - "9450:9450"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
