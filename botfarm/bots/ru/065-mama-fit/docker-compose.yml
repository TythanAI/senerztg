# Build from the botfarm root:  docker compose -f bots/ru/065-mama-fit/docker-compose.yml up -d
services:
  botfarm-ru-mama-fit:
    build:
      context: ../../..
      dockerfile: bots/ru/065-mama-fit/Dockerfile
    container_name: botfarm-ru-mama-fit
    restart: unless-stopped
    env_file: .env
    ports:
      - "9064:9064"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
