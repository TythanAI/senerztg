# Build from the botfarm root:  docker compose -f bots/ru/264-start/docker-compose.yml up -d
services:
  botfarm-ru-264-start:
    build:
      context: ../../..
      dockerfile: bots/ru/264-start/Dockerfile
    container_name: botfarm-ru-264-start
    restart: unless-stopped
    env_file: .env
    ports:
      - "9263:9263"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
