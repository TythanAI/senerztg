# Build from the botfarm root:  docker compose -f bots/ru/452-genshin-farm/docker-compose.yml up -d
services:
  botfarm-ru-452-genshin-farm:
    build:
      context: ../../..
      dockerfile: bots/ru/452-genshin-farm/Dockerfile
    container_name: botfarm-ru-452-genshin-farm
    restart: unless-stopped
    env_file: .env
    ports:
      - "9451:9451"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
