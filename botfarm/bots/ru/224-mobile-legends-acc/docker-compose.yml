# Build from the botfarm root:  docker compose -f bots/ru/224-mobile-legends-acc/docker-compose.yml up -d
services:
  botfarm-ru-224-mobile-legends-acc:
    build:
      context: ../../..
      dockerfile: bots/ru/224-mobile-legends-acc/Dockerfile
    container_name: botfarm-ru-224-mobile-legends-acc
    restart: unless-stopped
    env_file: .env
    ports:
      - "9223:9223"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
