# Build from the botfarm root:  docker compose -f bots/ru/494-video-edit-service/docker-compose.yml up -d
services:
  botfarm-ru-494-video-edit-service:
    build:
      context: ../../..
      dockerfile: bots/ru/494-video-edit-service/Dockerfile
    container_name: botfarm-ru-494-video-edit-service
    restart: unless-stopped
    env_file: .env
    ports:
      - "9493:9493"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
