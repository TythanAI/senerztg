# Build from the botfarm root:  docker compose -f bots/ru/382-bitget-acc/docker-compose.yml up -d
services:
  botfarm-ru-382-bitget-acc:
    build:
      context: ../../..
      dockerfile: bots/ru/382-bitget-acc/Dockerfile
    container_name: botfarm-ru-382-bitget-acc
    restart: unless-stopped
    env_file: .env
    ports:
      - "9381:9381"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
