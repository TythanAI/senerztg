# Build from the botfarm root:  docker compose -f bots/ru/379-okx-acc/docker-compose.yml up -d
services:
  botfarm-ru-379-okx-acc:
    build:
      context: ../../..
      dockerfile: bots/ru/379-okx-acc/Dockerfile
    container_name: botfarm-ru-379-okx-acc
    restart: unless-stopped
    env_file: .env
    ports:
      - "9378:9378"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
