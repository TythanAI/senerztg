# Build from the botfarm root:  docker compose -f bots/ru/385-kraken-acc/docker-compose.yml up -d
services:
  botfarm-ru-385-kraken-acc:
    build:
      context: ../../..
      dockerfile: bots/ru/385-kraken-acc/Dockerfile
    container_name: botfarm-ru-385-kraken-acc
    restart: unless-stopped
    env_file: .env
    ports:
      - "9384:9384"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
