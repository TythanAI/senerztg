# Build from the botfarm root:  docker compose -f bots/ru/282-openai-api/docker-compose.yml up -d
services:
  botfarm-ru-282-openai-api:
    build:
      context: ../../..
      dockerfile: bots/ru/282-openai-api/Dockerfile
    container_name: botfarm-ru-282-openai-api
    restart: unless-stopped
    env_file: .env
    ports:
      - "9281:9281"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
