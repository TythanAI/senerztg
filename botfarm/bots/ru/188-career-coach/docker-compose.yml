# Build from the botfarm root:  docker compose -f bots/ru/188-career-coach/docker-compose.yml up -d
services:
  botfarm-ru-career-coach:
    build:
      context: ../../..
      dockerfile: bots/ru/188-career-coach/Dockerfile
    container_name: botfarm-ru-career-coach
    restart: unless-stopped
    env_file: .env
    ports:
      - "9187:9187"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
