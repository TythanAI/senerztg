# Build from the botfarm root:  docker compose -f bots/ru/184-parent-school/docker-compose.yml up -d
services:
  botfarm-ru-184-parent-school:
    build:
      context: ../../..
      dockerfile: bots/ru/184-parent-school/Dockerfile
    container_name: botfarm-ru-184-parent-school
    restart: unless-stopped
    env_file: .env
    ports:
      - "9183:9183"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
