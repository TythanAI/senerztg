# Build from the botfarm root:  docker compose -f bots/ru/403-revolut-acc/docker-compose.yml up -d
services:
  botfarm-ru-403-revolut-acc:
    build:
      context: ../../..
      dockerfile: bots/ru/403-revolut-acc/Dockerfile
    container_name: botfarm-ru-403-revolut-acc
    restart: unless-stopped
    env_file: .env
    ports:
      - "9402:9402"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
