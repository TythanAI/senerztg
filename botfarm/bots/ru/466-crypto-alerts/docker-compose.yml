# Build from the botfarm root:  docker compose -f bots/ru/466-crypto-alerts/docker-compose.yml up -d
services:
  botfarm-ru-466-crypto-alerts:
    build:
      context: ../../..
      dockerfile: bots/ru/466-crypto-alerts/Dockerfile
    container_name: botfarm-ru-466-crypto-alerts
    restart: unless-stopped
    env_file: .env
    ports:
      - "9465:9465"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
