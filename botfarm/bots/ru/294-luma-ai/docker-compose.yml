# Build from the botfarm root:  docker compose -f bots/ru/294-luma-ai/docker-compose.yml up -d
services:
  botfarm-ru-294-luma-ai:
    build:
      context: ../../..
      dockerfile: bots/ru/294-luma-ai/Dockerfile
    container_name: botfarm-ru-294-luma-ai
    restart: unless-stopped
    env_file: .env
    ports:
      - "9293:9293"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
