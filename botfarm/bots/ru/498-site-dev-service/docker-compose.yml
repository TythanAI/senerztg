# Build from the botfarm root:  docker compose -f bots/ru/498-site-dev-service/docker-compose.yml up -d
services:
  botfarm-ru-498-site-dev-service:
    build:
      context: ../../..
      dockerfile: bots/ru/498-site-dev-service/Dockerfile
    container_name: botfarm-ru-498-site-dev-service
    restart: unless-stopped
    env_file: .env
    ports:
      - "9497:9497"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
