# Build from the botfarm root:  docker compose -f bots/ru/118-clean-service/docker-compose.yml up -d
services:
  botfarm-ru-118-clean-service:
    build:
      context: ../../..
      dockerfile: bots/ru/118-clean-service/Dockerfile
    container_name: botfarm-ru-118-clean-service
    restart: unless-stopped
    env_file: .env
    ports:
      - "9117:9117"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
