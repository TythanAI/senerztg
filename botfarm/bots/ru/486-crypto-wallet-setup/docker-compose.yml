# Build from the botfarm root:  docker compose -f bots/ru/486-crypto-wallet-setup/docker-compose.yml up -d
services:
  botfarm-ru-486-crypto-wallet-setup:
    build:
      context: ../../..
      dockerfile: bots/ru/486-crypto-wallet-setup/Dockerfile
    container_name: botfarm-ru-486-crypto-wallet-setup
    restart: unless-stopped
    env_file: .env
    ports:
      - "9485:9485"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
