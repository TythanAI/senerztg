# Build from the botfarm root:  docker compose -f bots/ru/497-bot-dev-service/docker-compose.yml up -d
services:
  botfarm-ru-497-bot-dev-service:
    build:
      context: ../../..
      dockerfile: bots/ru/497-bot-dev-service/Dockerfile
    container_name: botfarm-ru-497-bot-dev-service
    restart: unless-stopped
    env_file: .env
    ports:
      - "9496:9496"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
