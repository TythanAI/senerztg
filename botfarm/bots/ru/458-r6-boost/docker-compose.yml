# Build from the botfarm root:  docker compose -f bots/ru/458-r6-boost/docker-compose.yml up -d
services:
  botfarm-ru-458-r6-boost:
    build:
      context: ../../..
      dockerfile: bots/ru/458-r6-boost/Dockerfile
    container_name: botfarm-ru-458-r6-boost
    restart: unless-stopped
    env_file: .env
    ports:
      - "9457:9457"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
