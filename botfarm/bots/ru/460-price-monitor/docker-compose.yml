# Build from the botfarm root:  docker compose -f bots/ru/460-price-monitor/docker-compose.yml up -d
services:
  botfarm-ru-460-price-monitor:
    build:
      context: ../../..
      dockerfile: bots/ru/460-price-monitor/Dockerfile
    container_name: botfarm-ru-460-price-monitor
    restart: unless-stopped
    env_file: .env
    ports:
      - "9459:9459"
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
